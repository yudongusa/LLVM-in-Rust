# Rust build output
/target/
src/llvm-bench/target/

# Claude Code local-only files (not project-wide)
.claude/settings.local.json
.claude/worktrees/

# Differential fuzzing findings (uploaded as CI artifacts, not committed)
bench/fuzz/findings/

# macOS
.DS_Store
