interface:
  display_name: "Rust Stable Compat Agent"
  short_description: "Stable Rust migration workflow for issue #90 with PR review feedback"
  default_prompt: "Use $rust-stable-compat to implement issue #90 from audit through stable CI/docs updates, then review the PR and post review feedback before merge."
