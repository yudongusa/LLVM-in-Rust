[package]
name = "llvm-in-rust-ir-parser"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

description = "Lexer and parser for LLVM textual IR into LLVM-in-Rust data structures."
repository = "https://github.com/yudongusa/LLVM-in-Rust"
homepage = "https://github.com/yudongusa/LLVM-in-Rust"
keywords = ["llvm", "compiler", "ir", "codegen", "rust"]
categories = ["compilers", "development-tools"]
[lib]
name = "llvm_ir_parser"

[dependencies]
llvm-ir = { package = "llvm-in-rust-ir", version = "0.1.0", path = "../llvm-ir" }

[dev-dependencies]
llvm-ir         = { version = "0.1.0", package = "llvm-in-rust-ir", path = "../llvm-ir" }
llvm-codegen    = { version = "0.1.0", package = "llvm-in-rust-codegen", path = "../llvm-codegen" }
llvm-target-x86 = { version = "0.1.0", package = "llvm-in-rust-target-x86", path = "../llvm-target-x86" }
llvm-transforms = { version = "0.1.0", package = "llvm-in-rust-transforms", path = "../llvm-transforms" }
sha2            = "0.10"
hex             = "0.4"
serde_json      = "1"
